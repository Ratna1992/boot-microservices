spring:
  application:
    name: payment-service
  h2:
    console:
      enabled: true
  zipkin:
    base-url: http://localhost:9411/
  cloud:
    config:
      uri:
        - http://localhost:9093
server:
  port: 9091
logging:
    file:
        path: E:/logs/microservice